---
description: 验收分层 L1/L2/L3——声称「验收通过」或验证 UI/API 时必须遵守，禁止假阳性。
alwaysApply: false
---

# 验收测试

**禁止用代码审查代替运行验证。**

| 层 | 做什么 |
|---|---|
| L1 | tsc / lint / 审查 |
| L2 | 隔离测 API（curl/PS） |
| L3 | `npm run dev` 真实操作 UI |

## 强制

1. UI 验收必须 L3；console 红错 = 失败
2. API 可 L2，但须确认 Electron 能连上该 API
3. 启动竞态（backend 未就绪）是真 bug，不可「等一下就好」带过
4. 声称通过须附表：`标准 | L1 | L2 | L3 | 备注`；L2/L3 至少一项 ✅
