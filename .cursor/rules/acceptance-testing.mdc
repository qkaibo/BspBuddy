---
description: 验收测试方法论——定义什么是"验证通过"，防止假阳性验收
alwaysApply: true
---

# 验收测试标准

## 基本原则

**禁止用代码审查代替运行验证。** 代码看起来正确 ≠ 功能可用。

## 验收三层模型

| 层次 | 做什么 | 能发现什么 | 不能发现什么 |
|------|--------|-----------|-------------|
| L1 静态检查 | tsc --noEmit, linter, 代码审查 | 类型错误、语法问题 | 运行时错误、竞态条件、连接失败 |
| L2 隔离测试 | 单独启动后端 API 用 curl/PS 测试 | API 格式、认证、数据正确性 | 启动时序、Electron 集成、前端调用链 |
| L3 端到端测试 | 实际启动应用，操作用户界面 | **真实用户体验、console error、竞态条件** | — |

## 强制规则

1. **UI 功能验收必须走 L3**——实际 `npm run dev` 启动 Electron 应用，在界面上操作完整流程
2. **后端 API 验收可走 L2**——但必须确认 Electron 能启动到该 API（不能只测独立进程）
3. **console 报错视为失败**——DevTools console 中出现红色的 `ERR_CONNECTION_REFUSED`、`Uncaught TypeError` 等，即使功能"看起来"能用，也算验收不通过
4. **竞态条件是真实 bug**——启动时 backend 未就绪导致的前 6 秒连接拒绝，必须在验收时检出，不能靠"等一下就好了"带过

## 验收报告格式

每次 claim "验收通过" 时必须附带：

```
| 标准 | L1 | L2 | L3 | 备注 |
|------|:--:|:--:|:--:|------|
| xxx 功能 | ✅ | ✅ | ✅/❌ | 具体说明 |
```

L2 和 L3 至少一项必须为 ✅，才能打总体验收通过。

## 反面案例（本规则起因）

- L2 用 PowerShell 脚本测试 API 成功
- L3 从未实际启动 Electron
- 代码审查时看到 `EXPERT_FASTAPI_STATUS` 返回 `{ready: false}` 被当成 boolean 用，未发现
- 结论：声称"全部通过"，用户一启动就 `ERR_CONNECTION_REFUSED`
