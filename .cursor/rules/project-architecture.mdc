---
description: BspBuddy Electron 分层、品牌名与硬边界
alwaysApply: true
---

# 项目架构

Electron + React + TypeScript 桌面工作台。产品名 **BspBuddy** / `bspbuddy`——禁止新写或保留 `WorkBuddy` / `workbuddy`。

| 层 | 路径 | 职责 |
|---|---|---|
| Main | `src/main/` | services、tools、IPC、Python bridge |
| Preload | `src/preload/` | 仅 `contextBridge` → `invoke/on/off` |
| Renderer | `src/renderer/` `src/components/` `src/hooks/` | React UI |
| Shared | `src/lib/` | 类型、`IPC_CHANNELS`、client |

## 硬边界

- Renderer/hooks **禁止** Node API（`fs`、`child_process`、`electron.app`）
- UI 仅经 `createIpcClient()` + `IPC_CHANNELS`
- 领域类型：`src/lib/*-types.ts`；核心/Agent：`src/lib/types.ts`；别名 `@/*` → `src/`

脚手架 ≠ 已上线：改功能先查 `docs/README.md`；`specs/` 已废弃。
