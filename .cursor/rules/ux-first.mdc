---
description: UX 优先——能看到≠能用；禁止空数据假 UI、假交互、raw ID 输入
alwaysApply: true
---

# UX 优先

**能看到 ≠ 能用。**

## 禁止

- 空数据假 UI（handler 返回 `[]` → 永远「暂无」）
- Raw ID 输入绑定资源
- 假交互（按钮无真实效果）
- 硬编码选项（模型名等须从后端加载）
- 无加载态 / 无错误态

## 必须

- 数据源真实可用；开发期可用结构正确的 mock（≥3 条、含多状态），禁止 `return []`
- 选择器：浏览 → 选择 → 已选展示（名称非 ID）→ 移除；至少 2+ 可选项
- 空状态要有指引（或展示合理 mock），勿永久「暂无可用资源」

## 合并前

- [ ] 选择器 ≥3 条可选项
- [ ] 能完成搜索→绑定→移除
- [ ] 无空数组 IPC handler
- [ ] 有加载/错误 UI

布局原则见 `ui-design-principles` 与 `docs/reference/ref-001-ui-design-principles.md`。
