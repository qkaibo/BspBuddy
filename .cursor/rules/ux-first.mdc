---
description: 用户体验优先——组件必须可用，不可有占位假画面
alwaysApply: true
---

# UX 优先原则

## 核心准则

**能看到 ≠ 能用。** 组件渲染出 UI ≠ 用户能完成任务。

## 禁止事项

| 禁止 | 说明 | 例子 |
|------|------|------|
| 空数据假 UI | 选择器、列表永远显示"暂无数据"，因为数据源返回空数组 | SopSkillPicker 调 SOP_LIST 返回 [] → 用户永远看到"暂无可用资源" |
| Raw ID 输入 | 让用户手动输入资源 ID 来"绑定" | 输入框 + "添加 SOP ID" ——用户不知道 ID 是什么 |
| 假交互 | 按钮有 UI 但点击后什么都不发生 | "测试连接"永远返回"成功"，没有真实连接 |
| 硬编码选项 | 下拉写死几个模型名，不从后端加载 | `<select>` 里有 deepseek/hunyuan/kimi ——实际模型列表用户无权修改 |
| 无加载态 | 数据加载时无任何提示，界面空白 | useEffect 里 loading 状态不设，列表闪白 |
| 无错误态 | API 失败时不告知用户，静默失败 | catch 里只 console.log，用户不知道出错了 |

## 强制要求

### 1. 数据源必须真实可用

组件加载的数据必须来自一个可工作的 API/服务，且上线前验证过：

```
开发阶段:   IPC handler → 返回 mock 数据（结构正确、数据合理）
上线前:     IPC handler → 对接真实 FastAPI 端点
禁止:       IPC handler → return [] （用户看空列表）
```

### 2. 资源选择器 UX 最低标准

每个资源选择器必须满足：

- [ ] **浏览**：用户可以搜索、浏览已有资源的列表
- [ ] **选择**：用户可以勾选/点击添加资源
- [ ] **已选展示**：已绑定的资源显示名称+摘要信息，不是 raw ID
- [ ] **移除**：用户可以从已绑定列表移除资源
- [ ] **有数据**：加载的数据至少包含 2+ 条可选项（mock 或真实）

### 3. 空状态必须有意义

```
❌ "暂无可用资源" ← 永远这样，因为 API 返回空
✅ "暂无 SOP 技能，请先到 SOP 面板创建" ← 有指引
✅ 展示 3-5 条 mock 数据让用户理解这个功能怎么用
```

### 4. 对接真实 API 前用合理 mock

```
Mock 数据要求:
- 结构 100% 匹配真实 API 返回格式
- 至少 3 条数据，覆盖不同状态（启用/草稿/停用）
- 字段值看起来像真的（不是 "test1", "test2"）
- 在注释中标记 // TODO: replace with real API
```

## 反面案例（本规则起因）

```
ExpertEditorModal → SopSkillPicker Tab:
  - UI: 搜索框 + "已绑定 SOP"区域 + "可选 SOP"列表 ✅ 看起来不错
  - 实际: SOP_LIST handler 返回 [] → "暂无可用资源" ❌ 完全无法使用
  - 用户感受: 打开了编辑器，切换到 SOP 标签页，永远看不到任何 SOP 可选
```

同样的按钮假象也存在于 SkillPicker、KnowledgePicker、ConnectorPicker。

## 验收标准

功能合并前检查：
- [ ] 每个资源选择器至少有 3 条可选项数据
- [ ] 用户能完成"搜索→选择→绑定→查看→移除"完整流程
- [ ] 没有返回空数组的 IPC handler
- [ ] 加载态和错误态有 UI 提示

完整 UI 原则（布局/禁止纯表单页等）见 [`docs/reference/ref-001-ui-design-principles.md`](../../docs/reference/ref-001-ui-design-principles.md) 与 [`ui-design-principles.mdc`](./ui-design-principles.mdc)。本规则只管数据与交互可用性。
