---
description: 先文档后代码；以 docs/ 为准开发；specs/ 已废弃
alwaysApply: true
---

# ⛔ 文档前置规则（最高优先级，任何代码修改前强制执行）

## 负一号规则：强制操作顺序（机器可验证）

**每次要调用 StrReplace 或 Write 修改 `src/` 下的业务代码前，必须先调用 Read 打开对应的 `docs/` 文件。**

操作顺序固定为：

```
1. Read  docs/prd/xxx.md   （或 docs/plans/xxx.md）  ← 先打开文档
2. 如果文档缺少本次改动描述 → 先 StrReplace 补文档
3. StrReplace src/xxx.tsx                             ← 再改代码
```

**如果工具调用历史中出现 StrReplace/Write 修改了 src/ 文件，但在它之前没有 Read 对应的 docs/ 文件 → 违规。**

| 代码文件路径 | 必先读的 docs 路径 |
|-------------|-------------------|
| `src/components/ChatPanel.tsx` | `docs/plans/01-task-bar.md` 或 `docs/plans/agents-03-a2a-delegation.md` |
| `src/components/ConversationContextBar.tsx` | `docs/prd/agents-004-a2a-delegation.md` |
| `src/renderer/App.tsx` | `docs/prd/agents-004-a2a-delegation.md` |
| `src/hooks/useAgent.ts` | `docs/prd/agents-004-a2a-delegation.md` |
| `src/main/services/ipc-handlers.ts` | `docs/prd/agents-004-a2a-delegation.md` |

新增文件同样适用——先确保 docs 里有对应章节。


## 零号规则：改代码前必自问

**每次调用 Write / StrReplace / EditNotebook 前，必须先问自己：**

> "这个变动要到用户界面上吗？交互变了吗？数据模型变了吗？架构关系变了吗？"

**四者任一为「是」→ 先开 `docs/` 下的对应文件补文档，再改代码。不得先改代码后补文档。**

## 硬性门禁（读完后必须执行，不可跳过）

**使用 Write / StrReplace / EditNotebook 工具前，必须完成以下自检：**

1. 这次改动是否涉及以下任一情况？
   - 移动/添加/删除任何 UI 元素（按钮、选择器、面板位置变更）
   - 修改组件布局（flex、position、margin、padding 结构变化）
   - 新增或修改交互行为（点击/展开/切换/拖拽逻辑）
   - 新增 IPC 通道、API 端点、数据模型字段
   - 修改文案、placeholder、按钮文字（语义有变化时）

2. 如果上面任一答案为「是」→ **必须先打开并更新对应的 `docs/plans/` 或 `docs/prd/` 文件，然后再改代码。**

3. 如果跳过第 2 步直接改代码 → **本次改动无效，视为违规。**

## 判定矩阵

| 改动类型 | 示例 | 需要先更新 docs/ |
|---|---|---|
| 新增页面/组件/功能 | 新建 ExpertEditorModal | ✅ |
| **修改已有 UI 布局（重点）** | **按钮从上方移到左侧、控件从上方移到下方、flex 方向/对齐方式变更** | ✅ |
| 修改已有交互行为 | dropdown 改 modal、单选改多选 | ✅ |
| 新增 IPC 通道 / API 端点 | 新增 MODEL_CONFIG_LOCAL_SAVE | ✅ |
| 数据模型变更 | 加字段 `storage_mode` / `scope` | ✅ |
| 文案/提示语修改 | placeholder、按钮文字、错误提示（语义有变时） | ✅ |
| 修 crash / 运行时异常 | `ReferenceError: OpenAI is not defined` | ❌ |
| 修类型错误 / lint 错误 | `tsc --noEmit` 报错 | ❌ |
| 样式微调（不改布局结构） | 颜色、字号 ±2px、圆角、阴影 | ❌ |
| 重构（不改行为） | 提取函数、重命名变量 | ❌ |
| 依赖升级 | Electron v30 → v31 | ❌ |

> **任何涉及 `.tsx` 文件中组件位置/排列/布局结构的变更，都属于「修改已有 UI 布局」，必须先更新 plan 文档。**

---

## ⚠️ 违规信号

如果你正在做以下任一件事，**立即停止，回头补文档**：

- 打开了一个 `.tsx` 文件准备改 JSX 结构，但还没打开任何 `docs/` 下的文件
- 正在想「这个改动太小了不用写文档」
- 正在用 `StrReplace` 移动组件位置，但对应的 plan 文件没开
- 写完后才打开 `docs/` 目录

---

# 规格驱动开发（先文档后代码）

事实来源：`docs/`（`prd/`、`tech-spec/`、`plans/`、`reference/`）。索引与进度看板：`docs/README.md`。

**`specs/` 已废弃**——不要再读、不要再更新、不要把其中内容当产品真相。若 plan 头部仍写着旧 `specs/...` 路径，仅作历史溯源；实现与验收以 `docs/` 为准。

产品名是 **BspBuddy**。文档里若出现 WorkBuddy，视为上游参考，实现与对外文案一律用 BspBuddy，不要照抄品牌名。

---

## 硬性顺序：先文档，后开发

**禁止「想到哪做到哪」。** 没有对应的 `docs/prd/`（及按需的 tech-spec / plan）时，**不得**开写功能实现（业务 `tsx`/`ts`、IPC、服务）。

强制流程：

1. **读** `docs/README.md` —— 确认域内编号、状态、关联文档
2. **读/写 PRD** —— 含页面展现、字段、交互链（`→`）、跳转关系；缺则先补 PRD
3. **写/更新 Plan**（`docs/plans/`）—— 分期任务 + 改动范围
4. **必要时写 Tech Spec** —— API / 数据模型 / 流程（复杂后端或契约变更必写）
5. **再改代码** —— 行为以 `docs/` 为准，不本地另起炉灶
6. **行为真正符合 PRD/plan 后**，才更新 `docs/README.md` 状态

| 禁止 | 说明 |
|---|---|
| 无 PRD 直接写功能 | 先补 `docs/prd/{domain}-NNN-*.md` |
| 空脚手架标完成 | README / plan 不得把半成品标 ✅ |
| 用代码审查代替文档 | 文档缺口先补文档，再实现 |
| 复活 `specs/` | 新内容只进 `docs/` |

文档格式与模板见 `docs/STANDARD.md` 与规则 `documentation-standard.mdc`（二者对齐；冲突时以 `docs/STANDARD.md` 的领域分段编号为准）。

---

## 实现前（补充）

1. 先读 README 对应条目，再读相关 plan / PRD / Tech Spec
2. 以 `docs/` 中的产品选择为准，不要本地另起炉灶
3. 已修过的坑（禁止回退）：
   - Design → 只用 Ardot（不要换成 Excalidraw/tldraw/Figma）
   - Mailbox → Agent Mail 服务（不要做成通用 IMAP/SMTP）
   - 自动化推送 → 仅小程序（不要改成随意 IM/邮件）
   - Agent 权限模式 UI → 输入框下方下拉（plan 10；不要做成独立设置页）——**不是** auth RBAC
   - SOP 库 → 按用户权限隔离（「我的库」= 当前用户有权集合；不是本机全员共享）。见 agents-003 / agents-002-sop；专家 bindings 与库 edit 正交；**身份/角色从属 auth-001**
   - 全产品身份 / RBAC → auth-001 / auth-01（勿与 plan 10 Agent Permission Modes 混名或合并成「一个权限模块」）

## P3 优先级（来自 docs/README Plans 索引）

1. 06 插件生态 + 05 远程助理
2. 07 项目协作 + 08 连接器 + 09 自动化
3. **auth-01（身份/RBAC，auth-001）** + 10 Agent 权限模式（沙箱，勿与 auth 混名）+ 11 记忆 + 14 灵感
4. 12 创意画布 + 13 邮箱 + 15 企业智能体
5. 16 定价 + 17 数据设置 + 18 安装

Agents 域进行中：专家管理 / Scope 工作台 / **SOP 创作台（agents-02：编辑器 MVP 进行中，用户权限隔离 Phase E 待开发，依赖 auth-001 actor）**——实现前必须读对应 PRD；勿把本机共享库写成产品模型。

## 实现后

- 仅当行为真正符合 `docs/` 中 PRD / plan 时，才更新 `docs/README.md` 状态
- 空脚手架不要标成已完成
- UI 验收须遵守 `acceptance-testing.mdc`（L3），不得仅凭静态审查声称通过
