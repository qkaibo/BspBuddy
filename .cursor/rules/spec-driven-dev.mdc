---
description: 先文档后代码门禁；改 src/ 前必须先读/补 docs/
alwaysApply: true
---

# 文档前置门禁（最高优先级）

事实来源：`docs/`（`prd/`、`tech-spec/`、`plans/`、`reference/`）。索引：`docs/README.md`。
**`specs/` 已废弃**——勿读、勿更新、勿当产品真相。产品名 **BspBuddy**（勿照抄 WorkBuddy）。

## 强制顺序

改 `src/` 业务代码前：

```
1. Read docs/prd|plans 对应文件
2. 缺描述 → 先补文档
3. 再改代码
```

工具历史上若先 `Write`/`StrReplace` 了 `src/`、此前未 `Read` 对应 docs → **违规**。

## 改前自问（任一为是 → 先改 docs）

UI 元素增删移 / 布局结构变 / 交互变 / 新 IPC·API·模型字段 / 文案语义变？

| 需要先更新 docs | 可直接改代码 |
|---|---|
| 新功能、布局/交互变更、IPC/API/模型、语义文案 | crash/lint、样式微调、无行为重构、依赖升级 |

## 新功能流程

读 README → 补 PRD → 更新 Plan →（复杂则 Tech Spec）→ 再写代码 → 行为符合后再改 README 状态。禁止无 PRD 开写、空脚手架标 ✅、复活 `specs/`。

细则（文件↔docs 对照、历史坑、P3）见 `spec-driven-details`；文档格式见 `documentation-standard` / `docs/STANDARD.md`。
