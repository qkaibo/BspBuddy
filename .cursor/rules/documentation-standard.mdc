---
description: 文档目录、命名、front matter、PRD/Tech Spec 模板。编辑 docs/** 时遵守。
globs: docs/**
alwaysApply: false
---

# 文档规范

`specs/` 已废弃；新文档只写 `docs/`。先文档后代码见 `spec-driven-dev`。冲突时以 `docs/STANDARD.md` 为准。

## 目录

```
docs/
├── README.md / CONTEXT.md / STANDARD.md / WORKFLOW.md
├── prd/  tech-spec/  plans/  reference/
```

## 命名

`{domain}-{NNN}-{slug}.md`（域内不跳号）。例：`agents-001-expert-management.md`、plan 用两位 `agents-02-…`。

## Front matter

```yaml
---
id: agents-001
title: 中文标题
type: prd   # prd | tech-spec | plan | reference
related: []
---
```

## 模板要点

**PRD（8 章）**：概述、场景、功能、模型、页面与字段（ASCII 布局 + 字段表 + 交互链 `→`）、API、页面关系、验收。

**Tech Spec（7 章）**：概述、目标、API、模型、流程、安全、与 PRD 差异。

- 字段表 ≥4 列；交互用 `→`；字段必须对标真实模型，勿虚构。
