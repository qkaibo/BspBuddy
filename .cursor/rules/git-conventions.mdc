---
description: Git 分支命名与 Conventional Commits。创建分支、写 commit message 或开 PR 时使用。
alwaysApply: false
---

# Git 约定

## 分支

`<type>/<描述>`：`feature/` | `fix/` | `chore/` | `docs/`  
例：`feature/expert-management`、`fix/chat-crash`

## Commit（Conventional Commits）

`<type>: <英文简述>`，≤72 字符，小写开头。

| type | 用途 |
|---|---|
| `feat` / `fix` / `chore` / `docs` / `refactor` / `test` / `style` | 功能 / 修复 / 杂项 / 文档 / 重构 / 测试 / 格式 |

```
# ✅
feat: add expert editor with SOP binding
fix: resolve session save race condition

# ❌
feat: 添加专家编辑器
update code
fix: bug
```

## PR

从 `main` 拉出 → lint/构建通过 → 单域聚焦 → 合回 `main`。
