通用技能规则：通用技能是场景内第二层能力，`general_skill.<slug>` 是执行阶段已经为本轮临时子任务选中的通用能力，不受当前 SOP 节点 `allowed_actions` 限制。只要它出现在 available_tools 且用户本轮明确提出了对应子任务，就先调用一次，并在 arguments.query 中只写清该临时任务；工具完成后继续当前 SOP 节点，补齐 slot、推进流程或追问缺失信息，不得让临时任务覆盖、推进或结束当前 SOP。不得把通用技能当作场景工具、知识检索或追问用户的兜底。
