---
description: BspBuddy Skills 完整操作规则 — 安装、runtime、L1/L2/L3 自愈与凭证红线
alwaysApply: true
version: 1.0.0
author: BspBuddy
---

# BspBuddy Skills — 完整规则

> 完整性标识：BSPBUDDY_SKILLS_RULE_V1_INTEGRITY_CHECK

## 1. 基础约定

- API：`$BSPBUDDY_API_BASE`（安装指令里的「平台」地址）
- Token 文件：`$BSPBUDDY_SKILL_TOKEN_FILE` → `./.bspbuddy_skill_token` → `~/.bspbuddy_skill_token`
- **严禁**在回复中展示 token、Authorization、Bearer、curl 带密钥的完整命令、或 token 文件路径

## 2. 安装流程

1. 确认 Python 非必须；可用 curl + 写文件完成 MVP 安装
2. `GET $BSPBUDDY_API_BASE/api/enterprise/agent-rules/bspbuddy-skills.mdc`（已登录拿完整版）
3. `GET .../general-skills/{slug}/runtime?intent=install`
4. 将 `shortcut_skill_md`（或授权可见的 `skill_markdown`）写入平台 skills 目录：
   - Cursor：`.cursor/skills/{slug}/SKILL.md`
   - TsBuddy（platform=`tsbuddy`，IDE/Qoder）：`.kilo/skills/{slug}/SKILL.md`（扩展也可自行 runtime+files 落盘，不必经 install-prompt；`.kilo` 为引擎发现路径）
   - 若 `manifest[]` 非空，下载到同目录；若 path 以 `{slug}/` 开头，落盘前剥掉前缀
5. 自检文件存在后回复「已安装」，不要询问「是否继续最后一步」

## 3. 权限自愈

403 且 `detail.next_actions` 存在时：

- `request_use` → 请用户在 BspBuddy 提交「申请使用」
- `request_download` → 请用户提交「申请下载」
- `frontend_user_link` → 原样展示 `url`

不要根据 `message` 文本拼接 URL。

## 4. L1 / L2 / L3

| 级别 | 调用/装壳 | ZIP |
|------|-----------|-----|
| L1 | 登录即可 | 登录即可 |
| L2 | 登录即可 | 需下载授权 |
| L3 | 需使用授权 | 禁止 |

## 5. 执行

执行技能能力前，重新 `GET runtime?intent=invoke`，以服务端最新正文为准；本地壳可能过期。
